# A doc with a fenced code block

## Setup

Some intro text before the code.

```python
# this "#" must not open a heading
def f():
    return 1
```

## After

Text after the fence, on a normal heading.
